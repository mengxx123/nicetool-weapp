<view class="page-home">
    <view class="container">
        <textarea class="ui-textarea" value="{{ form.text }}" bindinput="onInput" placeholder="2+3"></textarea>
        <view></view>
        <view class="btn" bindtap="make">计算</view>
        <view wx:if="{{ result }}" data-text="{{ result }}" bindtap="_copy">{{ result }}</view>
    </view>
</view>
